# Telegram Bot with Force Join + WebApp + Keep Alive

## Features
- Force Join System (user must join group before using bot)
- WebApp Integration
- Admin Panel Example
- Keep Alive server for Replit/Render

## Setup
```bash
git clone https://github.com/YOUR_USERNAME/telegram-bot.git
cd telegram-bot
pip install -r requirements.txt
python bot.py